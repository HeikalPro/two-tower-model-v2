# Core dependencies
numpy>=1.24.0
pandas>=2.0.0

# PyTorch with CUDA support
# For CUDA 11.8 (most common):
# torch>=2.0.0+cu118 --index-url https://download.pytorch.org/whl/cu118
# torchvision>=0.15.0+cu118 --index-url https://download.pytorch.org/whl/cu118

# For CUDA 12.1:
# torch>=2.0.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# torchvision>=0.15.0+cu121 --index-url https://download.pytorch.org/whl/cu121

# For CPU only (fallback):
torch>=2.0.0
torchvision>=0.15.0

# Sentence transformers for text encoding
sentence-transformers>=2.2.0

# FAISS for vector search
# Note: faiss-gpu is not available via pip. Use one of these options:
# Option 1 (Recommended for GPU): Install via conda
#   conda install -c conda-forge faiss-gpu
# Option 2: Use CPU version (works fine for most use cases)
faiss-cpu>=1.7.4

# FastAPI for API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Configuration management
pyyaml>=6.0

# Utilities
tqdm>=4.66.0
scikit-learn>=1.3.0

